export const API_CONFIG = {
  auth: "/auth",
  users: "/users",
  roles: "/roles",
  leads: "/leads",
  activities: "/activities",
  tasks: "/tasks",
  applications: "/applications",
  dashboard: "/dashboard",
  agencies: "/consultancies",
  platform: "/platform",
} as const;
